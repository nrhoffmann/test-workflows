name: workflow a

on: status

jobs:
  one:
    runs-on: ubuntu-latest
    steps:
      - name: dump event
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo $GITHUB_CONTEXT